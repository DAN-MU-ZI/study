# 07. API 부하와 튜닝

다음 조건을 고정해 태그 검색 API의 기준 성능을 측정한다.

- 태그: `java`
- VU: 20
- 시간: 30초
- 페이지 범위: 0~19

기준 실행 후 `06`의 GIN 인덱스를 적용하고 같은 조건으로 다시 실행한다. k6의 요청 수·실패율·p95와 DB 보고서의 총 실행 시간·평균 실행 시간·블록 읽기·스캔 횟수를 비교한다.

추가 실험에서는 `DB_POOL_SIZE`를 5, 10, 30으로 바꾸되 한 번에 하나의 변수만 변경한다.

